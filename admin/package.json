{
  "name": "admin",
  "version": "0.0.1",
  "scripts": {
    "check-format": "prettier --check src",
    "format": "prettier --write src"
  },
  "dependencies": {
    "@iexec/dataprotector": "^2.0.0-beta.10"
  },
  "devDependencies": {
    "prettier": "^3.4.1"
  }
}
